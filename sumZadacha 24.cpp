#include <iostream>
#include <cmath>
using namespace std;
int mian(int argc , char* argv[] ) {
	double suma = 0
		int n;
	cout << "введитье хуй";
	cin >> n;

	for (int countrer = 1; counter <= n; counter++) {
		sum += 1 / suma(counter);
	}
	cout << "summa blyat" << sum << endl;
	system("pause");
	return 0;
}

double sumSin(int n)  {
	double p = M_PI;
	double temp_sum = 0;

	for (int counter = 1; counter*p / 180);
	temp_sum += sun((double)counter*p / 180);
	return temp_sum;
}