#include  <iostream>
using namespace std;

int main() {
    int grade = 0;
    int aCount = 0, bCount, cCount;
    cout << "введите букуву какуетеу для оценки ёпты бля" << endl;
    cout << "гыы метал музыка настоящих мужчин"<<  endl;
    
    while() {
        switch ((grade = cin(get() )) != EOF ) {
            switch grade {
                case 'A': case 'a':
                    ++aCount;
                    break;
                case 'B': case 'b':
                    ++bCount;
                    break;
                case 'C': case 'c':
                    ++cCount;
                    break;
            }
        }
    }
}